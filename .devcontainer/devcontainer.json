{
  "name": "python-for-machine-learning-and-data-science",
  "image": "mcr.microsoft.com/devcontainers/universal:2",
  "features": {
    "ghcr.io/devcontainers/features/python:1": {
      "version" : "3.12"
    }
  },
   "postCreateCommand": "pip install --upgrade pip && python -m venv .venv && . .venv/bin/activate && pip install uv && pip install -r requirements.txt",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python", // Example: Install the Python extension
        "ms-toolsai.jupyter"
      ]
    }
  }
}
